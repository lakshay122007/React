
//We cannot directly render the objects in react, we have to use array instead - i.e we cannot display obkect as a whole in react.
//UseState is imported from react and passes/return  an array, and introduce it with 'state variable' and 'state function'/\.
// App state management - use of UseState from react
import { UseState } from 'react'
- ***we use array destructuring and write a key word 'set' before defining oither key/property.***
-ex: //let [count, setCount] = useState(0)
//*** props are immutable ***

//Lifting State Up-
//App control
//Form input control


setFormData({...formData, [name] : e.target.value}) -----> rest form ka data get kra, then name field ke data ko e.target.value ke data se overwrite kr diya.


//next hook - 
-- > **useEffect()** - it handles all the side effects, such as fetching data etc.

suppose, to fetch, we create a function, but everytime it re renders,  the data is fetched again and again, 
to prevent this we it in within the 'useEffect' hook. 

flow - 1. the component renders ---> 2. fetch runs --> 3. setUsers(a useState or any data) updated --> 4.react re-renders ---> Fetch runs again. 
** THis goes in looop. from step 2 to 4 to sgain at 2.

useEffect(()=>{
    -------
},[]) --> the array passed as second argument is called "DEPENDENCY ARRAY" . (first argument is a callback function in useEffect())
therefore, if you want to run 1 time, the keep the array empty, i.e it dfoes not 'Depend' on other thing, and run only once.(doe not re renders again)
thus, we use fetch or any other external things inside the useEffect so that these things doesn't re-renders again.




//TAILWIND - Framework

- it have pre-defined Classes
- it has 3 part - 
1. Base : predefined base styling
2. component : predefined components
3. Utility : seperate properties for every css property(ex. .pt{
                                            padding-top:4rem -----> pt - 4 , this means padding from top with 4rem. 
                                        })




